test compile precise-output
set enable_simd
target x86_64 icelake-client

function %f1(i64) -> i64x2 {
block0(v0: i64):
  v1 = load.i64x2 v0
  v2 = iabs v1
  return v2
}

; VCode:
;   push rbp
;   mov rbp, rsp
; block0:
;   vpabsq xmm0, xmmword ptr [rdi + 0x0]
;   mov rsp, rbp
;   pop rbp
;   ret
; 
; Disassembled:
; block0: ; offset 0x0
;   push rbp
;   mov rbp, rsp
; block1: ; offset 0x4
;   vpabsq xmm0, xmmword ptr [rdi] ; trap: heap_oob
;   mov rsp, rbp
;   pop rbp
;   ret

