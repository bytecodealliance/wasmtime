test compile precise-output
target x86_64

function %f0(i8) -> i64 {
block0(v0: i8):
  v1 = sextend.i64 v0
  return v1
}

; VCode:
;   push rbp
;   mov rbp, rsp
; block0:
;   movsx rax, dil
;   mov rsp, rbp
;   pop rbp
;   ret
; 
; Disassembled:
; block0: ; offset 0x0
;   push rbp
;   mov rbp, rsp
; block1: ; offset 0x4
;   movsx rax, dil
;   mov rsp, rbp
;   pop rbp
;   ret

