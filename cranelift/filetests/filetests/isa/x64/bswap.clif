test compile precise-output
target x86_64

function %f0(i64) -> i64 {
block0(v0: i64):
  v1 = bswap v0
  return v1
}

; VCode:
;   push rbp
;   mov rbp, rsp
; block0:
;   mov rax, rdi
;   bswap rax, rax
;   mov rsp, rbp
;   pop rbp
;   ret
; 
; Disassembled:
; block0: ; offset 0x0
;   push rbp
;   mov rbp, rsp
; block1: ; offset 0x4
;   mov rax, rdi
;   bswap rax
;   mov rsp, rbp
;   pop rbp
;   ret

function %f1(i32) -> i32 {
block0(v0: i32):
  v1 = bswap v0
  return v1
}

; VCode:
;   push rbp
;   mov rbp, rsp
; block0:
;   mov rax, rdi
;   bswap eax, eax
;   mov rsp, rbp
;   pop rbp
;   ret
; 
; Disassembled:
; block0: ; offset 0x0
;   push rbp
;   mov rbp, rsp
; block1: ; offset 0x4
;   mov rax, rdi
;   bswap eax
;   mov rsp, rbp
;   pop rbp
;   ret

function %f2(i16) -> i16 {
block0(v0: i16):
  v1 = bswap v0
  return v1
}

; VCode:
;   push rbp
;   mov rbp, rsp
; block0:
;   mov rax, rdi
;   rol ax, ax, 0x8
;   mov rsp, rbp
;   pop rbp
;   ret
; 
; Disassembled:
; block0: ; offset 0x0
;   push rbp
;   mov rbp, rsp
; block1: ; offset 0x4
;   mov rax, rdi
;   rol ax, 8
;   mov rsp, rbp
;   pop rbp
;   ret

