test verifier
target x86_64

function %load_base_type(i64 vmctx) {
    template0 = vmctx
    template1 = load.i32 notrap aligned template0
    template2 = load.i32 notrap aligned template1 ; error: base template1 has type i32, which is not the pointer type i64

block0(v0: i64):
    return
}

function %template_wrong_type(i64 vmctx) {
    template0 = vmctx

block0(v0: i64):
    v1 = template.i32 template0 ; error: template instruction with type i32 references template with type i64
    return
}
